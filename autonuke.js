export async function main(ns) {
    ns.args;
    while (true) {
        ns.scan(ns.args[0]) = scanresults;
        ns.tprint("Scanned " + scanresults.length + " servers.");
        ns.tprint(scanresults);
    }
}
export async function main(ns) {
    const args = ns.args;
    print = ns.tprint;


    ns.tprint(`Number of arguments: ${args.length}`);
    ns.tprint(`First argument: ${args[0]}`);
    ns.tprint(ns.getServer(host?: string): args[0]);
}